<!-- ============================================================
     ПРИКЛАДИ ВИКОРИСТАННЯ КОНСТРУКТОРА МАПИ МІСТА
     ============================================================ -->

## 🎮 СЦЕНАРІЇ КОРИСТУВАННЯ

### Сценарій 1: Планування нового мікрорайону

1. **Спеціаліст відкриває сайт** → бачить порожну карту
2. **Вибирає інструмент "Дороги"** → режим активований (синя кнопка)
3. **Клікає на кілька місць на карті** → додаються дорожні сегменти
4. **Вибирає "Будинки"** → додає житлові будинки біля доріг
5. **Вибирає "Школи"** → додає навчальні заклади в центрі мікрорайону
6. **Вибирає "Лікарні"** → додає медичні заклади
7. **Вибирає "Парки"** → додає зелені зони для відпочинку
8. **Клікає "Зберегти план"** → дані зберігаються у браузері
9. **Перетягує об'єкти мишкою** → коригує позиції
10. **Видаляє помилкові об'єкти** → правий клік → Видалити
11. **Скасовує останню дію** → Ctrl+Z или кнопка "Скасувати"
12. **Закриває та повертається пізніше** → дані залишилися (localStorage)

### Сценарій 2: Аналіз структури мікрорайону

1. **Спеціаліст вже має план** → дані завантажуються з localStorage
2. **Бачить статистику** → 12 домів, 3 школи, 2 лікарні, 5 парків
3. **Потрібна підготовка звіту** → приховує дороги через фільтр
4. **Бачить виключно інфраструктуру** → легше аналізувати розташування
5. **Показує дороги назад** → включає фільтр для доріг
6. **Роздрукує план** → за допомогою Ctrl+P (CSS підтримує print)

---

## 🔧 ТЕХНІЧНІ ПРИМІТКИ

### Організація коду

```
index.html ────────┐
                   ├── css/style.css ────┐
                   │                     ├── [Вибір інструмента]
                   │                     ├── [Додавання об'єкта]
                   │                     └── [Рендеринг]
js/app.js ─────────┤
                   └── <СВГ холст>
                      ↓ (mousemove, click)
                   CityMapConstructor
                      ↓
                   localStorage
```

### Потік даних

```
User Input (click, drag)
    ↓
Event Handler
    ↓
this.objects (array mutation)
    ↓
this.history (save state)
    ↓
render() → SVG Elements
    ↓
Display Update
```

### Обмеження і граничні умови

1. **Максимум об'єктів** - нема (залежить від памяти браузера)
2. **Максимум кроків undo** - 50 кроків (можна змінити CONFIG)
3. **Розмір об'єкта** - залежить від типу (house: 40px, road: 50px)
4. **Межі холсту** - об'єкти не можна перетягувати за межі карти
5. **Фільтрація** - не видаляє об'єкти, тільки приховує

---

## 🐛 ВІДЛАДКА

### Як перевірити стан додатку в консолі браузера (F12)

```javascript
// Отримати посилання на екземпляр конструктора
// (Наразі закритий в замиканні, але можна модифікувати window)

// Перевірити localStorage
localStorage.getItem('cityMapConstructor_data')
// Результат: JSON масив об'єктів

// Очистити localStorage
localStorage.clear()

// Отримати статистику
console.log('Об\'єктів на карті:', 
    JSON.parse(localStorage.getItem('cityMapConstructor_data')).length)
```

### Типові проблеми і розв'язки

| Проблема | Причина | Розв'язок |
|----------|---------|-----------|
| Об'єкти не зберігаються | localStorage вимкнений | Включити у налаштуваннях браузера |
| Холст не реагує на клік | CSS display: none | Перевірити inspector |
| Drag & drop не працює | isDragging флаг | Перевірити консоль на помилки |
| Фільтри не працюють | filter checkbox не змінює стан | Перевірити event listeners |
| Undo кнопка неактивна | history массив пуст | Зробити якусь дію спочатку |

---

## 📱 АДАПТАЦІЯ ДЛЯ МОБІЛЬНИХ

Поточна реалізація оптимізована для дікстопа. Для мобільних можна:

1. **Замінити drag & drop на swipe жести**
   ```javascript
   element.addEventListener('touchstart', handleTouchStart);
   element.addEventListener('touchmove', handleTouchMove);
   element.addEventListener('touchend', handleTouchEnd);
   ```

2. **Збільшити розмір кнопок для пальця** (мінімум 44x44px)

3. **Змінити бокову панель на табulates**
   ```html
   <nav class="tabs">
       <button class="tab" data-tab="tools">Інструменти</button>
       <button class="tab" data-tab="filters">Фільтри</button>
       <button class="tab" data-tab="stats">Статистика</button>
   </nav>
   ```

4. **Додати touch feedback**
   ```css
   @media (hover: none) {
       .tool-btn:active {
           background-color: var(--primary-green);
       }
   }
   ```

---

## 🎨 КАСТОМІЗАЦІЯ

### Додати новий тип об'єкта (наприклад, "Церква")

1. **Оновити CONFIG в app.js**
   ```javascript
   church: {
       icon: '⛪',
       label: 'Церква',
       color: '#8B5A8B',
       size: 45,
   }
   ```

2. **Додати кнопку в HTML**
   ```html
   <button class="tool-btn" data-type="church">
       <i class="fas fa-gopuram"></i>
       <span>Церкви</span>
   </button>
   ```

3. **Додати фільтр**
   ```html
   <label class="filter-item">
       <input type="checkbox" data-filter="church" class="filter-checkbox" checked>
       <span class="filter-label">
           <i class="fas fa-gopuram" style="color: #8B5A8B;"></i>
           Церкви
       </span>
   </label>
   ```

4. **Оновити statistiku**
   ```javascript
   this.statsElements.churchCount = document.getElementById('churchCount');
   ```

5. **Додати HTML для статистики**
   ```html
   <div class="stat-item">
       <span class="stat-label">Церков:</span>
       <span class="stat-value" id="churchCount">0</span>
   </div>
   ```

Готово! Новий тип об'єкта працює.

---

## 🔐 БЕЗПЕКА

Поточна реалізація безпечна для локального використання:

- ✅ Немає вихідних запитів (без бекенду)
- ✅ Немає user input без обробки
- ✅ localStorage обмежений однією доменом
- ✅ СВГ елементи не можна виконати як код

Але для production:

1. Додати **XSS захист** (sanitize user input)
2. **Валідацію** даних перед загрузкою з localStorage
3. **HTTPS** для передачі чутливих даних
4. **Server-side storage** замість localStorage

---

## 📊 ПОКРАЩЕННЯ ПРОДУКТИВНОСТІ

Якщо планови мають 1000+ об'єктів:

1. **Використати Canvas замість SVG**
   - SVG гаразд для <500 об'єктів
   - Canvas краще для великих наборів даних

2. **Реалізувати spatial partitioning (quadtree)**
   - Прискорити пошук об'єктів при кліку
   - Оновлювати тільки видимі об'єкти

3. **Віртуальний скролл**
   - Для боку панелі зі списком об'єктів

4. **WebWorker для обробки даних**
   - Перенести розрахунки в фоновий потік

---

## 📚 РЕСУРСИ ДЛЯ НАВЧАННЯ

### Документація
- [MDN Web Docs - SVG](https://developer.mozilla.org/en-US/docs/Web/SVG)
- [MDN - localStorage API](https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage)
- [MDN - Event Handling](https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Building_blocks/Events)

### Статті про UX
- Nielsen Norman Group - 10 Usability Heuristics
- Don Norman - The Design of Everyday Things
- Steve Krug - Don't Make Me Think

### Інструменти
- [SVG Editor](https://boxy-svg.com/)
- [CSS Generator](https://www.cssdesigngenerator.com/)
- [Color Palette](https://coolors.co/)

---

## 🎓 НАВЧАЛЬНІ ЦІЛІ

Після вивчення цього коду ви розумієте:

1. ✅ Як організовувати код в класи
2. ✅ Як управляти станом додатку
3. ✅ Як обробляти складні системи подій
4. ✅ Як працювати з SVG графікою
5. ✅ Як використовувати Web APIs (localStorage, etc.)
6. ✅ Як проектувати UX з дотриманням евристик
7. ✅ Як писати читабельний, модульний код

---

**Успішного користування! 🚀**
