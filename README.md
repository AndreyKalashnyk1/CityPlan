# КОНСТРУКТОР МАПИ МІСТА - ДОКУМЕНТАЦІЯ

## 📋 СТРУКТУРА ПРОЄКТУ

```
e:\2 семестр\Лаба ЛМВЗ 4\
├── index.html          (HTML структура)
├── css/
│   └── style.css       (Стилізація)
├── js/
│   └── app.js          (Основна логіка)
```

---

## 🎨 РЕАЛІЗОВАНІ ФУНКЦІЇ

### 1. **Інтерактивна СВГ-карта**
- Порожня карта розташована в основній області екрана
- Підтримка 5 типів об'єктів міської інфраструктури
- Кожен об'єкт має унікальний колір, розмір та іконку

### 2. **Додавання об'єктів**
- Клік на кнопку в панелі інструментів активує режим додавання
- Клік на карту додає об'єкт у вибраній позиції
- Після додавання режим автоматично деактивується

### 3. **Drag & Drop маніпулювання**
- Об'єкти можна перетягувати мишкою
- При наведенні курсор змінюється на `grab`
- При перетягуванні об'єкт підсвічується (drop shadow)
- Об'єкти залишаються в межах карти (boundary detection)

### 4. **Фільтрація об'єктів**
- 5 чекбоксів для вмикання/вимикання типів об'єктів
- Фільтрація відбувається миттєво без перезавантаження
- Запам'ятовуються всі об'єкти, тільки приховуються

### 5. **Видалення об'єктів**
- **Правий клік** на об'єкті → модальне вікно видалення
- **Подвійний клік** на об'єкті → модальне вікно видалення
- **Delete** клавіша → видалення вибраного об'єкта
- Потім підтвердження в модальному вікні

### 6. **Скасування дій (Undo)**
- Кнопка "Скасувати" скасовує останню дію
- **Ctrl+Z** - гарячої клавіші для скасування
- Історія обмежена до 50 кроків
- Кнопка деактивується коли немає чого скасовувати

### 7. **Очищення карти**
- Кнопка "Очистити" видаляє всі об'єкти з поточною дією в історії

### 8. **Збереження у localStorage**
- Кнопка "Зберегти план" зберігає поточний стан
- Дані автоматично завантажуються при перезавантаженні сторінки
- Сховище: ключ `cityMapConstructor_data`

### 9. **Статистика в реальному часі**
- Лічильники для кожного типу об'єктів
- Загальна кількість об'єктів на карті
- Оновляються при кожній зміні

### 10. **Повідомлення (Feedback)**
- Зелене повідомлення внизу екрана
- "План успішно збережено!"
- "Об'єкт видалено"
- "Дія скасована"
- Автоматично зникає через 2 секунди

---

## 🎯 РЕАЛІЗОВАНІ UX ЕВРИСТИКИ

### 1. **Visibility of System Status** ✅
- Індикатор режиму показує активний інструмент
- Активна кнопка виділяється зеленим кольором з пульсуючою іконкою
- Курсор змінюється (crosshair → grab → default)
- Повідомлення про успішні дії

### 2. **Match with Real World** ✅
- Іконки (Font Awesome) відповідають реальним об'єктам
- Емодзі в СВГ-обектах: 🏠 🛣️ 🏫 🏥 🌲
- Назви українською мовою (перекладені)
- Колори логічні: червоний-дом, синій-дорога, оранжевий-школа

### 3. **User Control and Freedom** ✅
- Кнопка "Скасувати" - скасування дій
- Ctrl+Z - гарячі клавіші
- Клавіша Escape - закриття модального вікна
- Delete - видалення вибраного об'єкта
- Модальне вікно для підтвердження видалення

### 4. **Consistency** ✅
- Однакові стилі всіх кнопок (округлі кути, тіні)
- Однакові переходи (transitions) при наведенні
- Єдина палітра кольорів по всему интерфейсу
- Однакові розміри іконок та елементів

### 5. **Error Prevention** ✅
- Модальне вікно при видаленні об'єкта
- Підтвердження при очищенні всієї карти
- Граничні умови (boundary detection) при перетягуванні
- Валідація фільтрів

### 6. **Flexibility & Efficiency** ✅
- Кілька способів видалення (правий клік, подвійний клік, Delete)
- Гарячі клавіші (Ctrl+Z)
- Режим додавання можна скасувати натиском Escape

---

## 🎨 ДИЗАЙН

### Кольорова схема (суміжна)
```
Основні:
- Зелений: #52C41A (парки, кнопка збереження)
- Синій: #4A90E2 (дороги, основний accent)
- Сірий: #F0F2F5 (фон, нейтральні елементи)

Об'єкти:
- Будинки (Дом): #FF6B6B (червоний)
- Дороги: #4A90E2 (синій)
- Школи: #F5A623 (оранжевий)
- Лікарні: #D84449 (карміновий)
- Парки: #52C41A (зелений)
```

### Макет
- **Header**: Gradient (синій → зелений), breadcrumbs, заголовок
- **Sidebar**: 300px ширина, прокручувана область, 4 секції
- **Main Content**: SVG холст, адаптивна ширина
- **Desktop**: Flex макет
- **Mobile**: Горизонтальна прокрутка sidebar

### Типографія
- Шрифт: System UI (San Francisco, Segoe UI, Roboto)
- Базовий розмір: 16px
- Заголовки: 600-700 вага
- Текст: 400 вага

---

## 💻 ТЕХНІЧНІ ДЕТАЛІ

### HTML (index.html)
- Семантична структура (header, aside, main)
- SVG холст для малювання об'єктів
- Модальне вікно для видалення
- Внутрішні повідомлення про дії
- Breadcrumbs навігація

### CSS (style.css)
- 500+ рядків з коментарями
- CSS змінні для кольорів
- Flexbox + Grid розташування
- Плавні переходи (transitions) та анімації
- Адаптивна верстка (@media queries)
- Стилі для СВГ-об'єктів

### JavaScript (app.js)
- Клас `CityMapConstructor` з методами
- Управління станом додатку (objects, history, filters)
- Обробка подій (click, mousemove, mouseup)
- Функції рендеринга СВГ
- localStorage API для збереження
- Keyboard shortcuts обробка

---

## 🎮 УПРАВЛІННЯ

### Миша
1. **Клік на кнопку інструмента** → активує режим додавання
2. **Клік на карту** → додає об'єкт
3. **Наведення на об'єкт** → виділення (drop shadow)
4. **Перетягування об'єкта** → Drag & Drop
5. **Правий клік на об'єкт** → видалення
6. **Подвійний клік на об'єкт** → видалення

### Клавіатура
- **Ctrl+Z** → скасування (Undo)
- **Delete** → видалення вибраного об'єкта
- **Escape** → закриття модального вікна / вихід з режиму

### Кнопки інтерфейсу
- **Будинки** / **Дороги** / **Школи** / **Лікарні** / **Парки** → вибір інструмента
- **Чекбокси фільтру** → вмикання/вимикання видимості
- **Зберегти план** → збереження у localStorage
- **Скасувати** → undo останньої дії
- **Очистити** → видалення всіх об'єктів

---

## 📊 СТРУКТУРА JAVASCRIPT КОДА

### Клас CityMapConstructor
```javascript
- this.objects          // Масив об'єктів на карті
- this.history          // Історія дій для undo
- this.selectedTool     // Вибраний інструмент
- this.selectedObject   // Вибраний об'єкт
- this.isDragging       // Флаг перетягування
- this.filters          // Об'єкт фільтрів
- this.canvasWidth      // Ширина холсту
- this.canvasHeight     // Висота холсту
```

### Основні методи
```javascript
init()                  // Ініціалізація
selectTool()           // Вибір інструмента
addObject()            // Додавання об'єкта
selectObject()         // Виділення об'єкта
deleteObject()         // Видалення об'єкта
saveHistory()          // Збереження в історію
undo()                 // Скасування дії
toggleFilter()         // Вмикання/вимикання фільтру
saveToStorage()        // Збереження у localStorage
loadFromStorage()      // Завантаження з localStorage
updateStats()          // Оновлення статистики
render()               // Рендеринг холсту
```

---

## 🚀 ЗАПУСК

### Локально через браузер
1. Відкрити `index.html` у браузері (double-click)
   
### Через локальний сервер (рекомендується)
```bash
# Python 3.7+
python -m http.server 8000 --directory "."

# Потім перейти на http://localhost:8000
```

```bash
# Node.js (якщо встановлен)
npx http-server
```

---

## ✅ ЧЕКЛИСТ ВИМОГ

### Функціональні вимоги
- ✅ Інтерактивна СВГ-карта без бекенду
- ✅ Додавання 5 типів об'єктів
- ✅ Drag & Drop маніпулювання
- ✅ Фільтрація об'єктів
- ✅ Збереження у localStorage
- ✅ Скасування дій (undo)
- ✅ Видалення об'єктів
- ✅ Статистика в реальному часі

### UX/UI вимоги
- ✅ Visibility of system status
- ✅ Match with real world
- ✅ User control and freedom
- ✅ Consistency
- ✅ Error prevention
- ✅ Flexibility & efficiency
- ✅ Tooltip-підказки (title атрибути)
- ✅ Breadcrumbs в шапці

### Візуальні вимоги
- ✅ Суміжна кольорова схема (зелений, синій, сірий)
- ✅ Мінімалістичний дизайн
- ✅ Адаптивна верстка
- ✅ CSS Grid + Flexbox
- ✅ Font Awesome іконки

### Технічні вимоги
- ✅ HTML5 + CSS3 + Vanilla JavaScript
- ✅ Без бекенду
- ✅ Чиста структура проєкту
- ✅ Коментарі в коді
- ✅ Читабельний код

---

## 🎓 НАВЧАЛЬНА ЦІННІСТЬ

Цей проект демонструє:

1. **Управління станом** - об'єкти, історія, фільтри
2. **DOM маніпулювання** - додавання/видалення елементів
3. **Event обробка** - click, mousemove, mouseup, keyboard
4. **СВГ графіка** - створення і рендеринг фігур
5. **localStorage API** - збереження даних
6. **UX дизайн** - евристики юзабіліті
7. **CSS верстка** - Flexbox, Grid, responsive design
8. **Об'єктно-орієнтоване програмування** - клас з методами

---

## 📝 ПРИМІТКИ

- Холст автоматично адаптується до розміру вікна
- Всі об'єкти зберігаються у форматі JSON
- Межі карти запобігають вихідцю об'єктів за межі
- Фільтри не видаляють об'єкти, тільки приховують їх
- Історія обмежена до 50 кроків для оптимізації памяті
- Код готовий до розширення (додавання нових типів об'єктів)

---

**Розроблено: Грудень 2025**
**Дисципліна: Проєктування інтерфейсів користувача**
