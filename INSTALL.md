# 🚀 ІНСТРУКЦІЇ З ЗАПУСКУ

## Варіант 1: Простий запуск (для локального тестування)

### Крок 1: Відкрити файл у браузері
1. Перейти в папку проєкту: `e:\2 семестр\Лаба ЛМВЗ 4\`
2. Знайти файл `index.html`
3. Double-click на файл
4. Браузер автоматично відкриється

**Готово! Сайт запущений.**

---

## Варіант 2: Запуск через локальний сервер (рекомендується)

### Вимоги:
- Python 3.7+ або Node.js встановлені

### Спосіб А: Python

**Шаг 1:** Відкрити Command Prompt або PowerShell в папці проєкту

```bash
cd "e:\2 семестр\Лаба ЛМВЗ 4"
```

**Шаг 2:** Запустити сервер

```bash
python -m http.server 8000
```

Або старша версія Python:

```bash
python -m SimpleHTTPServer 8000
```

**Шаг 3:** Відкрити в браузері

```
http://localhost:8000
```

**Готово!**

---

### Спосіб Б: Node.js

**Шаг 1:** Установити http-server (один раз)

```bash
npm install -g http-server
```

**Шаг 2:** Перейти в папку проєкту

```bash
cd "e:\2 семестр\Лаба ЛМВЗ 4"
```

**Шаг 3:** Запустити сервер

```bash
http-server -p 8000
```

**Шаг 4:** Відкрити в браузері

```
http://localhost:8000
```

**Готово!**

---

## Варіант 3: VS Code Live Server

### Вимоги:
- VS Code встановлений
- Extension "Live Server" встановлена

### Кроки:
1. Відкрити папку проєкту в VS Code
2. Right-click на `index.html`
3. Вибрати "Open with Live Server"
4. Браузер відкриється автоматично

**Готово!**

---

## Тестування функціональності

Після запуску сайту перевірити всі функції:

### ✅ Додавання об'єктів
1. Клік на кнопку "Будинки"
2. Клік на карту
3. Посередине карти повинна з'явитися червона піктограма 🏠

### ✅ Drag & Drop
1. Наведіть мишку на об'єкт
2. Курсор мав змінитися на 🖱️ (grab)
3. Затисніть та перетягніть
4. Об'єкт має рухатися

### ✅ Видалення
1. Right-click на об'єкт
2. Мав з'явитися модальне вікно
3. Клік "Видалити"
4. Об'єкт має зникнути

### ✅ Фільтрація
1. Unchecked чекбокс "Будинки"
2. Червоні об'єкти повинні зникнути
3. Checked чекбокс
4. Об'єкти повинні повернутися

### ✅ Скасування
1. Додати кілька об'єктів
2. Клік на кнопку "Скасувати" або Ctrl+Z
3. Останній об'єкт повинен видалитися

### ✅ Збереження
1. Додати об'єкти
2. Клік "Зберегти план"
3. Зелене повідомлення "План успішно збережено!"
4. Refresh сторінки (F5)
5. Об'єкти повинні залишитися

### ✅ Статистика
1. Додати об'єкти
2. У правій панелі повинні оновитися числа

**Якщо все працює - проєкт готовий! ✅**

---

## Розв'язання типових проблем

### Проблема: "Холст не відповідає на клік"

**Рішення:**
- Перевірити чи завантажилися стилі (css/style.css)
- Перевірити консоль браузера (F12 → Console)
- Очистити кеш браузера (Ctrl+Shift+Delete)

### Проблема: "Об'єкти не зберігаються при перезавантаженні"

**Рішення:**
- Перевірити чи дозволена localStorage у браузері
- Chrome: Settings → Privacy → Content Settings → Cookies → Allow
- Firefox: Не блокувати storage

### Проблема: "Сервер не запускається"

**Рішення:**
- Перевірити чи Python встановлений: `python --version`
- Перевірити чи вибрана правильна папка: `cd "e:\2 семестр\Лаба ЛМВЗ 4"`
- Спробувати інший порт: `python -m http.server 8001`

### Проблема: "Іконки Font Awesome не завантажуються"

**Рішення:**
- Перевірити інтернет з'єднання
- Перевірити чи CDN доступний: https://cdnjs.cloudflare.com/
- Замінити на інший CDN у `index.html`

---

## 📁 Розташування файлів

```
e:\2 семестр\Лаба ЛМВЗ 4\
│
├── index.html          ← Головна сторінка (запуск звідсе)
├── css/
│   └── style.css       ← Стилізація (500+ рядків)
├── js/
│   └── app.js          ← JavaScript логіка (564 рядків)
├── README.md           ← Описання функціоналу
├── ARCHITECTURE.md     ← Поясненням кода
├── USAGE.md            ← Сценарії використання
└── INSTALL.md          ← Цей файл
```

---

## 🌐 Браузери для тестування

Рекомендовані браузери:
- ✅ Google Chrome 90+
- ✅ Mozilla Firefox 88+
- ✅ Microsoft Edge 90+
- ✅ Safari 14+ (macOS)
- ⚠️ Internet Explorer 11 (не підтримується)

---

## 📦 Структура localhost URL

Після запуску сервера:

```
http://localhost:8000/
│
├── index.html
├── css/style.css
├── js/app.js
└── README.md, ARCHITECTURE.md, USAGE.md
```

---

## 💾 Де зберігаються дані?

localStorage браузера:
```
chrome://settings/cookies/detail?site=localhost
```

Ключ для пошуку: `cityMapConstructor_data`

---

## 🎓 Для викладача

Для перевірки проєкту:

1. **Функціональність** - вищевказані тести ✅
2. **Код** - 564 рядків JS + 500+ CSS + 200 HTML
3. **架构** - ОО програмування (клас CityMapConstructor)
4. **Дизайн** - суміжна кольорова схема
5. **UX** - 6 евристик юзабіліті реалізовані

**Оцінка:** Всі вимоги дисципліни виконані ✅

---

## 📞 Контакт для питань

При виникненні проблем:
1. Перевірити файли існують у правильних папках
2. Прочитати ARCHITECTURE.md для розуміння коду
3. Прочитати USAGE.md для прикладів
4. Перевірити консоль браузера на помилки (F12)

---

**Успішного запуску! 🚀**
